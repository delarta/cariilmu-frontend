const initState = {
  mentor: {},
  classes: [],
  students: []
};

const mainReducer = (state = initState, action) => {
  switch (action.type) {
    
    default:
      return state;
  }
};

export default mainReducer